import { SDKOptions, ValidationRules } from '@bugfender/common';
import { IBrowser } from './browser';
import { ISDKPrivateOptions } from './types/sdk-options';
export declare class SDKOptionsValidator {
    private readonly browser;
    protected rules: ValidationRules;
    constructor(browser: IBrowser);
    init(options: SDKOptions): ISDKPrivateOptions;
}
