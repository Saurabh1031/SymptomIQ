import { LogEntryShort } from '@bugfender/common';
import { ICallInfo } from './types/error';
import { Time } from './timer';
import { NullableDeviceKeyValue } from './types/device';
import { LogEntryString } from './storage-writer';
export declare class LogFactory {
    private maxLogLength;
    constructor(maxLogLength?: number);
    createFromDeviceKey(key: string, value: NullableDeviceKeyValue, callInfo: ICallInfo, time: Time): LogEntryShort;
    createFromEntry(log: LogEntryString, callInfo: ICallInfo, time: Time): LogEntryShort;
    createFromIssue(issueUUID: string, callInfo: ICallInfo, time: Time): LogEntryShort;
    createGapLog(logs: LogEntryShort[]): LogEntryShort;
}
