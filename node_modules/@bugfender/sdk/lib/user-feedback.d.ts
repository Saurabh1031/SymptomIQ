import { UserFeedbackOptions, UserFeedbackResult } from '@bugfender/common';
import { CommandsQueue } from './commands-queue';
export declare class UserFeedback {
    protected readonly commands: CommandsQueue;
    private modal?;
    private deferred?;
    private optionsValidator;
    constructor(commands: CommandsQueue);
    protected cancel(): void;
    protected drop(): void;
    protected resolve(result: UserFeedbackResult): void;
    show(options?: UserFeedbackOptions): Promise<UserFeedbackResult>;
    protected submit(subject: string, feedback: string): Promise<void>;
}
