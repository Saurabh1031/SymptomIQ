import { ICurrentSession } from './current-session';
import { Database } from './data/database';
import { IAPIFeatureDetection } from './feature-detection/api-feature-detection';
export interface IResetStorage {
    resetIfAppChanged(appID: string): Promise<void>;
}
export declare class ResetStorage implements IResetStorage {
    private readonly apiFeatures;
    private readonly session;
    private readonly database;
    private readonly localStorage;
    private readonly sessionStorage;
    constructor(apiFeatures: IAPIFeatureDetection, session: ICurrentSession, database: Database, localStorage: Storage, sessionStorage: Storage);
    resetIfAppChanged(appID: string): Promise<void>;
}
