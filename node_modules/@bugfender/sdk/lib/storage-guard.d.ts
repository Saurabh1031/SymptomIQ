import { IApiClient } from './api-client';
import { IDevice } from './device';
import { ILogger } from './logger';
import { ISDKPrivateOptions } from './types/sdk-options';
export interface IStorageGuard {
    canProcess(): boolean;
    canWrite(): boolean;
    setDeviceForceEnabled(state: boolean): void;
}
export declare class StorageGuard implements IStorageGuard {
    protected api: IApiClient;
    protected device: IDevice;
    protected logger: ILogger;
    protected options: ISDKPrivateOptions;
    protected deviceForceEnabled: boolean;
    protected isAppValid: boolean;
    protected isDeviceEnabled: boolean;
    constructor(api: IApiClient, device: IDevice, logger: ILogger, options: ISDKPrivateOptions);
    canProcess(): boolean;
    canWrite(): boolean;
    protected checkIsDeviceEnabled(): Promise<void>;
    setDeviceForceEnabled(state: boolean): void;
}
