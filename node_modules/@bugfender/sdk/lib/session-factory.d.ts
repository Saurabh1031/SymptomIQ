import { IDevice } from './device';
import { ISDKPrivateOptions } from './types/sdk-options';
import { ISession } from './types/session';
export declare class SessionFactory {
    protected device: IDevice;
    protected sdkVersion: number;
    protected window: Window;
    constructor(device: IDevice, sdkVersion: number, window: Window);
    create(uuid: string, options: ISDKPrivateOptions): ISession;
}
