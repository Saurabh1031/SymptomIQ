export interface IAPIFeatureDetection {
    isBroadcastChannelSupported(): Promise<boolean>;
    isIndexedDBSupported(): Promise<boolean>;
    isLocalStorageSupported(): boolean;
    isSessionStorageSupported(): boolean;
    isWorkerSupported(): boolean;
}
export declare class APIFeatureDetection implements IAPIFeatureDetection {
    private readonly indexedDB;
    private readonly localStorage;
    private readonly sessionStorage;
    private readonly broadcastChannel;
    private readonly worker;
    constructor(indexedDB: IDBFactory, localStorage: Storage, sessionStorage: Storage, broadcastChannel: typeof window.BroadcastChannel | undefined, worker: typeof window.Worker | undefined);
    isBroadcastChannelSupported(): Promise<boolean>;
    isIndexedDBSupported(): Promise<boolean>;
    isLocalStorageSupported(): boolean;
    isSessionStorageSupported(): boolean;
    isWorkerSupported(): boolean;
    private isStorageSupported;
}
