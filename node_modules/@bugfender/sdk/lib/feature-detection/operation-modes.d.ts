import { IAPIFeatureDetection } from './api-feature-detection';
export declare enum OperationMode {
    A = "A",
    B = "B",
    C = "C"
}
export declare class OperationModes {
    private readonly apiFeatures;
    constructor(apiFeatures: IAPIFeatureDetection);
    private hasLeaderElection;
    private hasSharedDB;
    private hasSharedDeviceInfo;
    getMode(): Promise<OperationMode>;
    isMultiTab(): Promise<boolean>;
    isMultiThread(): Promise<boolean>;
}
