export interface Time {
    iso: string;
    epoch: number;
}
export interface ITimer {
    getTime(): Time;
}
export declare class Timer implements ITimer {
    private getNow;
    protected lastAbsoluteTime: number;
    constructor(getNow?: () => Date);
    getTime(): Time;
}
