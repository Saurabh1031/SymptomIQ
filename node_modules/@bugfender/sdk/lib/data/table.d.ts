import { IDBPDatabase } from 'idb/with-async-ittr';
export declare class Table<Add, T> {
    private readonly dbPromise;
    private readonly store;
    constructor(dbPromise: Promise<IDBPDatabase>, store: string);
    add(data: Add): Promise<void>;
    clear(): Promise<void>;
    countBy(key: string, value: string): Promise<number>;
    deleteBatch(offset: number, limit: number, where?: [string, unknown]): Promise<void>;
    deleteByIdx(id: string | number): Promise<void>;
    getAll(): Promise<T[]>;
    getBatch(sessionUUID: string, batchSize: number): Promise<T[]>;
    patchByIdx(id: string | number, data: Partial<T>): Promise<void>;
}
