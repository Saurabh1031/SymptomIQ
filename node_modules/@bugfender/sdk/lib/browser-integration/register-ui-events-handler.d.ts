import { CommandsQueue } from '../commands-queue';
import { JsEventUi, JsEventUiType, RegisterEventsHandlerBase } from './register-events-handler.base';
export declare class RegisterUIEventsHandler extends RegisterEventsHandlerBase {
    protected commands: CommandsQueue;
    protected window: Window;
    constructor(commands: CommandsQueue, window: Window);
    init(): void;
    private sendKeyupLog;
    protected getEventData(target: Element, eventName: JsEventUiType): JsEventUi;
}
