import { NullableDeviceKeyValue } from '../types/device';
import { JsEventLogEntry } from './register-events-handler.base';
export declare class EventPrinter {
    private readonly console;
    private readonly printToConsole;
    private readonly styles;
    constructor(console: Console, printToConsole: boolean);
    private print;
    printDeviceKey(key: string, value: NullableDeviceKeyValue): void;
    printJsEvent(log: JsEventLogEntry): void;
    private truncate;
}
