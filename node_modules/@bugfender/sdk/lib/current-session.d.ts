import { IUUIDFactory } from './uuid-factory';
import { IDevice } from './device';
import { ISessionsTable } from './data/sessions.table';
import { ILogger } from './logger';
export interface ICurrentSession {
    getUUID(): string;
    reset(): void;
}
export declare class CurrentSession implements ICurrentSession {
    protected device: IDevice;
    protected logger: ILogger;
    protected sessionStorage: Storage;
    protected sessionsTable: ISessionsTable;
    protected uuidFactory: IUUIDFactory;
    protected uuid: string;
    constructor(device: IDevice, logger: ILogger, sessionStorage: Storage, sessionsTable: ISessionsTable, uuidFactory: IUUIDFactory);
    getUUID(): string;
    protected initSession(): void;
    reset(): void;
}
