import { SDKOptions } from '@bugfender/common';
type NativeOnlyOptions = 'maximumLocalStorageSize' | 'enableLogcatLogging';
export interface ISDKPrivateOptions extends Required<Omit<SDKOptions, NativeOnlyOptions>> {
    appID: string;
}
export {};
