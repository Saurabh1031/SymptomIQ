export declare class HttpClient {
    protected readonly apiURL: string;
    protected readonly appKey: string;
    protected readonly sdkUserAgent: string;
    constructor(apiURL: string, appKey: string, sdkUserAgent: string);
    post<T = any>(endpoint: string, payload: Record<string, any>): Promise<T>;
}
