import { BugfenderFacade } from '../bugfender';
/**
 * Registers a global error handler that will trigger a crash report in Bugfender
 * @internal
 */
export declare class RegisterErrorHandler {
    private readonly window;
    private rejectionsMap;
    private bugfender;
    constructor(window: Window);
    init(bugfender: BugfenderFacade): void;
    private logError;
}
